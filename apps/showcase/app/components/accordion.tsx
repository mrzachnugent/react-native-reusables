import { AccordionPreview } from '@/registry/examples/accordion';
import { View } from 'react-native';

export default function AccordionScreen() {
  return (
    <View className="items-center px-6">
      <AccordionPreview />
    </View>
  );
}
