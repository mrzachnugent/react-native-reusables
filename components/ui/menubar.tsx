import { Text } from 'react-native';

// TODO: Use https://zeego.dev/ which requires native code (does not work with Expo Go)
// Expo Go: unsure
export function Menubar() {
  return <Text className='text-foreground'>Menubar</Text>;
}
