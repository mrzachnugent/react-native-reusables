import { Text, View } from 'react-native';

export default function HoverCardScreen() {
  return (
    <View className='flex-1 justify-center items-center'>
      <Text className='text-foreground'>
        Hover Card (web) / Popover (native)
      </Text>
    </View>
  );
}
